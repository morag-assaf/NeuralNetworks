class classification_model(object):
    def __init__(self,train_x):
        self.train_x=train_x
        ## - probably other parameters as well##

    def nn(self):
        ## - definition of the model ##
        #add the network model and layers for example Sequential

    def fit(self):
        # use the self.nn() network
        # compile
        # model.fit
        # compute training validation set

    def initilize_parameters(self):
        #not sure what they are but usually we need it

    def predict(self):
        #given a trained model, predict Y

    def evaluate(self):
        #depends how we evaluate the model
        #use built function from Keras model


    def optimize_net(self):
        parameters=...
        self.fit
    ...
